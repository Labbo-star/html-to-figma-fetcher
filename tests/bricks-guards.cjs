const fs=require('fs'),vm=require('vm'),assert=require('assert/strict');const src=fs.readFileSync(require('node:path').join(__dirname,'../api/render7.js'),'utf8');const code=src.slice(src.indexOf('async function prepareBricksPage'),src.indexOf('const corePath'));
class E {constructor(cls,blocked=false,clip='auto'){this.cls=cls;this.blocked=blocked;this.attrs={};this.props={};this.parentElement=null;this.clip=clip;this.style={setProperty:(k,v)=>this.props[k]=v};}closest(s){return this.blocked?this:null;}setAttribute(k,v){this.attrs[k]=v;}removeAttribute(k){delete this.attrs[k];}}
const allowed=new E('anim-opacity'),popup=new E('anim-opacity',true),slide=new E('anim-opacity',true),word=new E('brf-split');word.attrs['aria-hidden']='true';word.parentElement=allowed;const service=new E('screen-reader',false,'rect(1px, 1px, 1px, 1px)'),visibleLink=new E('skip-link');const root={contains:()=>true,querySelectorAll:s=>s.includes('[aria-hidden')?[word]:[allowed,popup,slide,word]};
const ctx={HTMLElement:E,document:{body:{classList:{contains:()=>true}},querySelector:()=>root,querySelectorAll:()=>[service,visibleLink]},window:{},getComputedStyle:e=>({display:'block',visibility:'visible',clip:e.clip,clipPath:'none'}),setTimeout};vm.createContext(ctx);vm.runInContext(code,ctx);
(async()=>{const r=await ctx.prepareBricksPage(true);assert.equal(allowed.props.opacity,'1');assert.equal(word.attrs['aria-hidden'],undefined);assert.equal(popup.props.opacity,undefined);assert.equal(slide.props.opacity,undefined);assert.equal(service.props.display,'none');assert.equal(visibleLink.props.display,undefined);console.log('PASS reveal visible content, keep popup and inactive slide hidden, retain visible accessibility links');ctx.document.body.classList.contains=()=>false;assert.equal(await ctx.prepareBricksPage(false),null);console.log('PASS non-Bricks path unchanged');})();
